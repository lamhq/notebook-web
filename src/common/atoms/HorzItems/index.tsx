import { styled } from '@material-ui/core/styles';
import Box from '@material-ui/core/Box';

export const HorzItems = styled(Box)({
  gap: '10px',
  display: 'flex',
  alignItems: 'center',
});

export default HorzItems;
